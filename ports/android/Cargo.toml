[package]
name = "android_port"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
client = { path = "../../client/" }
slint = { version = "1.6", features = ["backend-android-activity-06"] }
